<UserControl x:Class="Home.Views.Dialogs.EditWaitUcView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Home.Views.Dialogs"
             mc:Ignorable="d" 
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes" Height="300" Width="400">
    <Grid Background="#FF323232">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Text="修改待办事项"  Foreground="White" FontSize="20" Padding="20 0" FontWeight="Bold"/>
        <DockPanel Grid.Row="1"  LastChildFill="False">
            <StackPanel Margin="20 10" DockPanel.Dock="Top" Orientation="Horizontal">
                <TextBlock Text="状态:" Foreground="White" VerticalAlignment="Center"></TextBlock>
                <ComboBox Foreground="White" Margin="10 0 0 0" SelectedIndex="{Binding WaitInfo.Status}">
                    <ComboBoxItem Content="未开始" />
                    <ComboBoxItem Content="已完成" />
                </ComboBox>
            </StackPanel>
            <TextBox Text="{Binding WaitInfo.Title}" Foreground="White" md:HintAssist.Hint="代办事项概要"  DockPanel.Dock="Top" Margin="20 0"></TextBox>
            <TextBox Text="{Binding WaitInfo.Content}" Foreground="White" MinHeight="100" md:HintAssist.Hint="内容"  DockPanel.Dock="Top" Margin="20 0"></TextBox>
        </DockPanel>
        <StackPanel Margin=" 10" Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <Button Margin="10 0" Content="确定" Command="{Binding SaveCommand}"></Button>
            <Button Margin="10 0" Content="取消" Command="{Binding CancelCommand}"></Button>
        </StackPanel>
    </Grid>
</UserControl>
