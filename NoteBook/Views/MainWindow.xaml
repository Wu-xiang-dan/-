<Window x:Class="NoteBook.Views.MainWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:prism="http://prismlibrary.com/"
       xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
       xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
       prism:ViewModelLocator.AutoWireViewModel="True"
       Title="{Binding Title}" Height="700" Width="1200"
       TextElement.Foreground="{DynamicResource MaterialDesignBody}"
       Background="{DynamicResource MaterialDesignPaper}"
       
        >
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1"/>
    </WindowChrome.WindowChrome>

    <materialDesign:DialogHost DialogTheme="Inherit" Identifier="RootDialog">

        <materialDesign:DrawerHost IsLeftDrawerOpen="{Binding ElementName=MenuToggleButton, Path=IsChecked}" x:Name="drawerHost">
            <!--左侧菜单-->
            <materialDesign:DrawerHost.LeftDrawerContent>
                <DockPanel MinWidth="220">
                    <StackPanel DockPanel.Dock="Top">
                        <Image Source="/Image/user.jpg" Width="50" Height="50">
                            <Image.Clip>
                                <EllipseGeometry Center="25,25" RadiusX="22" RadiusY="22"></EllipseGeometry>
                            </Image.Clip>
                        </Image>
                    </StackPanel>
                    <!--菜单列表-->
                    <ListBox ItemsSource="{Binding LeftMenuList}" ItemContainerStyle="{StaticResource LeftMenuStyle }" x:Name="MenuLb" SelectionChanged="lbMenu_SelectionChanged">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding NavigateCommand}" 
                          CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ListBox},Path=SelectedItem}">

                                </i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Background="Transparent">
                                    <materialDesign:PackIcon Kind="{Binding Icon}" Margin="5 0"></materialDesign:PackIcon>
                                    <TextBlock Text="{Binding MenuName}" Margin="5 0 "></TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </materialDesign:DrawerHost.LeftDrawerContent>
            <!--头部-->
            <DockPanel>
                <materialDesign:ColorZone Padding="16" MouseDoubleClick="ColorZone_MouseDoubleClick"
                                 materialDesign:ElevationAssist.Elevation="Dp4"
                                 DockPanel.Dock="Top"
                                 Mode="PrimaryMid">
                    <DockPanel LastChildFill="True">
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                            <Image Source="/Image/user.jpg" Width="50" Height="50">
                                <Image.Clip>
                                    <EllipseGeometry Center="25,25" RadiusX="22" RadiusY="22"></EllipseGeometry>
                                </Image.Clip>
                            </Image>
                            <Button x:Name="btnmin" Content="{materialDesign:PackIcon Kind=WindowMinimize}" Style="{StaticResource MaterialDesignFlatMidBgButton}" Click="btnmin_Click"></Button>
                            <Button x:Name="btnmax" Content="{materialDesign:PackIcon Kind=WindowMaximize}" Style="{StaticResource MaterialDesignFlatMidBgButton}" Click="btnmax_Click"></Button>
                            <Button x:Name="btnclose" Content="{materialDesign:PackIcon Kind=WindowClose}" Style="{StaticResource MaterialDesignFlatMidBgButton}" Click="btnclose_Click"></Button>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <ToggleButton x:Name="MenuToggleButton"
                           AutomationProperties.Name="HamburgerToggleButton"
                           IsChecked="False"
                           Style="{StaticResource MaterialDesignHamburgerToggleButton}" />

                            <Button Margin="24,0,0,0" Command="{Binding GoBackCommand}"
                     materialDesign:RippleAssist.Feedback="{Binding RelativeSource={RelativeSource Self}, Path=Foreground}"
                     Content="{materialDesign:PackIcon Kind=ArrowLeft,
                                                       Size=24}"
                     Foreground="{Binding RelativeSource={RelativeSource AncestorType={x:Type FrameworkElement}}, Path=(TextElement.Foreground)}"
                     Style="{StaticResource MaterialDesignToolButton}"
                     ToolTip="Previous Item" />

                            <Button Margin="16,0,0,0" Command="{Binding GoForwardCommand}"
                     materialDesign:RippleAssist.Feedback="{Binding RelativeSource={RelativeSource Self}, Path=Foreground}"          
                     Content="{materialDesign:PackIcon Kind=ArrowRight,
                                                       Size=24}"
                     Foreground="{Binding RelativeSource={RelativeSource AncestorType={x:Type FrameworkElement}}, Path=(TextElement.Foreground)}"
                     Style="{StaticResource MaterialDesignToolButton}"
                     ToolTip="Next Item" />
                        </StackPanel>


                      <TextBlock Margin="-152,0,0,0"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      AutomationProperties.Name="Material Design In XAML Toolkit"
                      FontSize="22"
                      Text="Material Design In XAML Toolkit" />
                    </DockPanel>
                </materialDesign:ColorZone>
                <!--区域-->
                <ContentControl prism:RegionManager.RegionName="ContentRegion"/>
            </DockPanel>
        </materialDesign:DrawerHost>
    </materialDesign:DialogHost>
</Window>
