<UserControl x:Class="NoteBook.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NoteBook.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:extent="clr-namespace:NoteBook.Extension"
             Height="450" Width="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Image Width="300" Source="/Image/db.png" />
        <!--显示信息-->
        <materialDesign:Snackbar x:Name="ReLoginBar" Grid.ColumnSpan="2" MessageQueue="{materialDesign:MessageQueue}">
        </materialDesign:Snackbar>
        
        <materialDesign:Transitioner Grid.Column="1" SelectedIndex="{Binding SelectedIndex,FallbackValue=0}">
            <materialDesign:TransitionerSlide>
                <DockPanel Margin="15" VerticalAlignment="Center">
                    <TextBlock Text="欢迎使用" FontSize="24" FontWeight="Bold" DockPanel.Dock="Top" ></TextBlock>
                    <TextBox materialDesign:HintAssist.Hint="请输账号" Text="{Binding Account,Mode=TwoWay,UpdateSourceTrigger=LostFocus}" DockPanel.Dock="Top" Margin="0 5"></TextBox>
                    <PasswordBox materialDesign:HintAssist.Hint="密码" extent:PassWordBoxExtend.Password="{Binding Password, Mode=OneWayToSource,UpdateSourceTrigger=LostFocus}"  DockPanel.Dock="Top"  Margin=" 0 5">
                        <i:Interaction.Behaviors>
                            <extent:PasswordBoxBehavior/>
                        </i:Interaction.Behaviors>
                    </PasswordBox>
                    <Button Content="登入" DockPanel.Dock="Top" Margin="0 5" Command="{Binding LoginCommand}"></Button>
                    <DockPanel Margin="0 5" LastChildFill="False">
                        <TextBlock Text="注册账号" DockPanel.Dock="Left">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="MouseLeftButtonDown">
                                    <i:InvokeCommandAction Command="{Binding ShowRegistInfoCommand}"/>                                                                         
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBlock>
                        <TextBlock Text="找回账号" DockPanel.Dock="Right">
                            
                        </TextBlock>
                    </DockPanel>
                </DockPanel>
            </materialDesign:TransitionerSlide>
        
            <materialDesign:TransitionerSlide>
                <DockPanel Margin="15" VerticalAlignment="Center">
                    <TextBlock Text="注册账号" FontSize="24" FontWeight="Bold" DockPanel.Dock="Top" ></TextBlock>
                    <TextBox materialDesign:HintAssist.Hint="请输姓名" Text="{Binding Accountinfo.AccountName}" DockPanel.Dock="Top" Margin="0 5"></TextBox>
                    <TextBox materialDesign:HintAssist.Hint="请输账号" Text="{Binding Accountinfo.Account}" DockPanel.Dock="Top" Margin="0 5"></TextBox>
                    <PasswordBox materialDesign:HintAssist.Hint="请输入密码" extent:PassWordBoxExtend.Password="{Binding Accountinfo.Password, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" DockPanel.Dock="Top"  Margin=" 0 5">
                        <i:Interaction.Behaviors>
                            <extent:PasswordBoxBehavior/>
                        </i:Interaction.Behaviors>
                    </PasswordBox>
                    <PasswordBox materialDesign:HintAssist.Hint="再次确认密码" extent:PassWordBoxExtend.Password="{Binding Accountinfo.ComfirmPassword, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" DockPanel.Dock="Top"  Margin=" 0 5">
                        <i:Interaction.Behaviors>
                            <extent:PasswordBoxBehavior/>
                        </i:Interaction.Behaviors>
                    </PasswordBox>
                    <Button Content="注册账号" Command="{Binding RegisterCommand}" DockPanel.Dock="Top" Margin="0 5"></Button>
                    <Button Content="返回登入" DockPanel.Dock="Top" Command="{Binding ShowLoginInfoCommand}" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                </DockPanel>
            </materialDesign:TransitionerSlide>

        </materialDesign:Transitioner>
    </Grid>
</UserControl>
